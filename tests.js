"use strict";

const should = require('should');
const _ = require('underscore');
const co = require('co');

describe('RML8', () => {

    it('installation', () => co(function*() {
			const duniter = require('duniter');
			const server = duniter({ memory: true });
    }));
});
